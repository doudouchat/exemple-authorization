hazelcast:
   cluster-name: "authorization-cluster"
   network:
      port:
         port: ${hazelcast.port}
         auto-increment: true
      join:
         tcp-ip:
            enabled: false
         multicast:
            enabled: false